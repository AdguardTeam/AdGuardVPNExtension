/* eslint-disable import/no-cycle */
// Global connectivity service instance.
export { connectivityService } from './main';
// Connectivity service event types and constants.
export { ConnectivityEventType, MIN_CONNECTION_DURATION_MS } from './fsm';
