.search {
    position: relative;

    &__input {
        display: block;
        width: 100%;
        height: 45px;
        padding: 0 20px;
        border-radius: 7px;
        background-color: var(--grayF7);
        border: none;
        font-size: 15px;

        &:focus {
            outline: none;
        }
    }

    /*FIXME style*/
    &__clear {
        position: absolute;
        right: 10px;
        top: 7px;
    }

    /*FIXME styles*/
    &__cross {
        width: 24px;
        height: 24px;
    }

    &__nothing-found {
        text-align: center;
        margin-top: 120px;
    }
}
