.state-checkbox {
    margin-right: 16px;
    padding: 0;
    background: none;
    border: none;
    cursor: pointer;
    max-height: 24px;

    &__list-item {
        margin-right: 12px;
    }

    &__icon {
        width: 24px;
        height: 24px;
    }
}
