.modal {
    &__title {
        letter-spacing: 0;
        margin: 10px 0 25px;
        font-size: 34px;
        font-weight: bold;
        line-height: 30px;
    }
}

.mode-select-button {
    border: none;
    background: none;
    padding: 10px 20px;
    border-radius: 3px;
    cursor: pointer;

    &.enabled {
        color: var(--grayF3);
        background-color: var(--gray-base);
    }
}
