.search {

    &__input {
        display: block;
        width: 100%;
        height: 45px;
        padding: 0 20px;
        border-radius: 7px;
        background-color: var(--grayF7);
        border: none;
        font-size: 15px;

        &:focus {
            outline: none;
        }
    }
}
