.radio {
    display: flex;
    align-items: center;
    cursor: pointer;
    margin-bottom: 35px;
    border: none;
    padding: 0;
    background: none;

    &__icon {
        flex-shrink: 0;
        width: 24px;
        height: 24px;
        margin: 0 15px 0 0;
        color: var(--green700);
    }

    &__title {
        font-size: 16px;
    }

    &__description {
        font-size: 1.3rem;
        line-height: 1.1;
        color: var(--gray88);
    }
}
