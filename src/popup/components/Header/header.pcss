.header {
    position: relative;
    z-index: var(--header-z-index);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 14px 24px;

    &__setting {
        width: 24px;
        height: 24px;
        padding: 0;
    }
}
