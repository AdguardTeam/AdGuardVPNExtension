.header {
    position: relative;
    z-index: var(--header-z-index);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 14px;

    &__title {
        display: flex;
        align-items: center;
    }

    &__logo {
        width: 153px;
        height: 28px;
        color: var(--brand-primary);
    }

    &__setting {
        width: 24px;
        height: 24px;
        padding: 0;
    }
}
