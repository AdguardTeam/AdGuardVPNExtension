.hint-popup {
    width: 220px;
    border-radius: 8px;
    margin: auto;
    padding: 15px;
    background-color: var(--white);
    text-align: left;
    position: absolute;
    left: 0;
    right: 0;
    bottom: 204px;

    &:after {
        content: "▼";
        color: var(--white);
        z-index: 2;
        display: block;
        position: absolute;
        bottom: -17px;
        right: 47%;
        font-size: 23px;
    }

    &__content {
        font-size: 14px;
    }

    &__button {
        margin-top: 15px;
    }
}
