.map {
    padding: 20px 0;
    min-height: 200px;
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: space-between;
}

.current-status {
    font-size: 18px;
    font-weight: bold;
}

.change-location {
    text-decoration: underline;
}
