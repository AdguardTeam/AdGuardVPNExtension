.map {
    padding: 20px 0;
    min-height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    overflow: hidden;
    position: relative;
}

.current-status {
    font-size: 18px;
    font-weight: bold;
}

.change-location {
    text-decoration: underline;
}
