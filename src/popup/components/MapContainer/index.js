import React from 'react';
import Map from './Map';
import SiteInfo from './SiteInfo';

const MapContainer = () => {
    return (
        <>
            <SiteInfo />
            <Map />
        </>
    );
};

export default MapContainer;
