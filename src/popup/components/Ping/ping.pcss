.ping {
    margin-left: auto;
    margin-right: 0;
    font-size: 16px;
    line-height: 20px;
    text-align: right;

    &--success {
        color: var(--text-links-link-default);
    }

    &--warning {
        color: var(--text-links-attention-link-default);
    }
}
