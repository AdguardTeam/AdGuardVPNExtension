.ping {
    margin-left: auto;
    margin-right: 0;
    font-size: 16px;
    line-height: 20px;
    text-align: right;

    &--success {
        color: var(--green700);
    }

    &--warning {
        color: var(--orange);
    }
}
